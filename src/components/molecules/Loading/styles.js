import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
  progress: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    textAlign: 'center',
  },
  fixed: {
    position: 'fixed',
    height: '100%',
    width: '100%',
  },
})
