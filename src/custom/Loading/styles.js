import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    progress: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: 400,
        textAlign: 'center',
    },
});
